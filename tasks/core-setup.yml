- name: Install System Packages 2 (dpkg, snapd, curl, wget, tree)
  become: true
  apt:
      name: ["dpkg", "snapd", "curl", "wget", "tree"]

- name: Install System Packages 1 (trash-cli, flameshot, preload, htop)
  become: true
  apt:
      name: ["trash-cli", "flameshot", "preload", "htop"]

- name: Install System Packages 3 (tree, python, docker.io)
  become: true
  apt:
      name: ["tree", "python", "docker.io"]

- name: Install System Packages 4 (kitty, i3, rofi, compton, compton, i3blocks, feh)
  become: true
  apt:
      name: ["kitty", "i3", "rofi", "compton", "compton", "i3blocks", "feh"]
