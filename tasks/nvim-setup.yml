---
- name: Install nvim plugins
  ansible.builtin.command: nvim --headless -c "autocmd User PackerComplete quitall" -c "PackerSync"
  changed_when: false
  tags: [install, nvim-setup, nvim]
#
# - name: Install nvim treesitter parsers
#   shell: nvim --headless -c "TSInstallSync all" -c q
#   tags: *tags
